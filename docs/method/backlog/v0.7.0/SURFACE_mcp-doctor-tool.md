# MCP doctor tool

Wire `runDiagnostics()` from `src/doctor.js` into the MCP server as a
`doctor` tool. No required params. Returns the same checks array as
`--json --doctor`.

Debt from cycle 0005-think-doctor.
