# Tune hotkey ergonomics

Allow the macOS app to override the default global hotkey while keeping a sane built-in default. Keep preference plumbing lightweight; do not add a settings window until it is clearly earned. If configuration is added, include basic validation and collision awareness.
