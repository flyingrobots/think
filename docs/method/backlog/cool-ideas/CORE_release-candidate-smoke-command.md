# Release-candidate smoke command

Add one deterministic repo command that runs the minimum credible pre-release checks for Think's real surfaces: CLI, MCP, graph migration, and platform-aware macOS verification.

This should be smaller than the full suite and more honest than an ad hoc checklist.
