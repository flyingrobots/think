# Writer provenance views

Expose provenance by ingress surface in a way that is useful rather than substrate-heavy. Examples: "Captured from menu bar", "Captured from CLI", "Explored during brainstorm."

Only show up once it helps reflective understanding and does not drag Git/WARP concepts into normal UX.
