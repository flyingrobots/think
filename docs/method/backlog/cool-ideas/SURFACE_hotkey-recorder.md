# Hotkey recorder

If configurable hotkeys ship, use a focused recorder flow instead of a settings-heavy surface. One narrow responsibility, basic collision detection, no preference sprawl, no panel clutter.
