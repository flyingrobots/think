# Thought graph visualization

Render the worldline as a DAG in the terminal. Show capture sessions
as clusters, reflect derivations as edges, temporal flow as layout.

Could use bijou's braille chart infrastructure for sub-character
resolution. The browse TUI could summon this as a panel alongside
the reader-first view.
