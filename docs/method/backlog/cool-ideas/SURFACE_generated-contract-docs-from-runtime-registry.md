# Generated contract docs from runtime registries

Think already has strong runtime seams around CLI and MCP. Use those to generate human-readable contract docs and snapshot tests from the same registry, rather than hand-maintaining CLI examples and MCP schema prose separately.
