# Session replay

Play back a capture session chronologically, like watching yourself
think. Show each thought appearing in sequence with real timing
gaps (or accelerated). Useful for reviewing a thinking session's
arc — what started it, where it went, what emerged.

Could be a TUI mode or a CLI command that streams to stdout.
