# --remember --global flag

`think --remember` defaults to ambient project recall, which
surprises users expecting global search. Add a `--global` flag
to force cross-project search across all minds.

Source: code-quality audit 2026-04-11 §1.2.
