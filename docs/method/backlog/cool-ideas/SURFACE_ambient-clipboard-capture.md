# Ambient clipboard capture

macOS watcher that captures clipboard changes as thoughts. Opt-in,
not default. When you copy a snippet that looks like an idea or
note, it gets captured with provenance showing the source app.

Pairs with the existing macOS menu bar app. Could be a toggle
in the menu bar — "Watch clipboard."
