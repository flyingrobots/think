# Generated surface capability matrix

Generate one matrix that shows each Think capability across CLI, `--json`, MCP, and macOS, along with the owning runtime contract.

Derive it from registries or tests so it stays honest.
