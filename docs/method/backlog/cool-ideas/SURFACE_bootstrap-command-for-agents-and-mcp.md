# Bootstrap command for agent and MCP setup

Think should be able to print or write its own local bootstrap artifacts: a shell-callable `think` wrapper, an isolated `THINK_REPO_DIR`, and example MCP config blocks for supported agent hosts.

That would turn the current manual wrapper-script dance into a product capability.
