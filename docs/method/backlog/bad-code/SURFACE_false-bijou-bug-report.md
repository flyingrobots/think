# False bug report filed at bijou

`~/git/bijou/docs/method/feedback/think-bg-fill-regression-v4.4.0.md`
reports a bg fill regression that was actually Think's fault (not
passing ctx to createFramedApp). The report should be archived with
a disposition note explaining the root cause.
