# Raise SSJR grades for `src/cli/help.js`

Current SSJR sanity check: `Hex B`, `P1 B`, `P2 C`, `P3 B`, `P4 C`, `P6 B`.

Help is still mostly a string registry with conventions around topics and commands. Tighten the boundary between command definition and help rendering so the text surface derives from one runtime-backed source of truth.
