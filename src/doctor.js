/**
 * Core diagnostic logic for think --doctor.
 * Pure, testable — no CLI or MCP dependencies.
 */

export async function runDiagnostics({ thinkDir: _thinkDir, repoDir: _repoDir, upstreamUrl: _upstreamUrl = '' } = {}) {
  // TODO: implement
  await Promise.resolve();
  return { checks: [] };
}
